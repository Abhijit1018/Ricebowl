/**
 * Where the Rice Bowl website gets its live menu and sends its orders.
 * Edit this file (no rebuild needed) if the backend URL or the outlet code changes.
 * The outlet code is shown in the POS under Settings.
 */
window.RICE_BOWL_CONFIG = {
  apiBase: 'https://cafepos-backend-3xyu.onrender.com/api',
  outlet: 'rice-bowl',
  currency: '₹',
};
